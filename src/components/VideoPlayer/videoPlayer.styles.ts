import styled from "styled-components";

export const RetroTV = styled.div`
  iframe {
    border-radius: 8px;
    position: relative;
    z-index: 1;
    box-shadow:
      inset 0 0 50px rgba(0, 255, 0, 0.1),
      0 0 30px rgba(0, 255, 0, 0.3),
      0 10px 30px rgba(0, 0, 0, 0.5);
  }
`;
